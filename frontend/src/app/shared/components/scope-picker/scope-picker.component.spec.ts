import { TestBed } from '@angular/core/testing';import { of } from 'rxjs';import { ScopePickerComponent } from './scope-picker.component';import { CatalogApiService } from '../../../core/api/catalog-api.service';
describe('ScopePickerComponent',()=>{it('loads authorized projects on initialization',async()=>{const api={projects:()=>of([{id:'p1',name:'Demo Marketplace'}]),environments:()=>of([]),servers:()=>of([]),instances:()=>of([])};await TestBed.configureTestingModule({imports:[ScopePickerComponent],providers:[{provide:CatalogApiService,useValue:api}]}).compileComponents();const fixture=TestBed.createComponent(ScopePickerComponent);fixture.detectChanges();expect(fixture.componentInstance.projects()).toHaveLength(1);});});
