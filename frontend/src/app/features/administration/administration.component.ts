import { Component,inject,OnInit,signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { ButtonModule } from 'primeng/button';import { SelectModule } from 'primeng/select';import { TableModule } from 'primeng/table';import { InputTextModule } from 'primeng/inputtext';import { MessageModule } from 'primeng/message';import { CatalogApiService } from '../../core/api/catalog-api.service';import { EnvironmentView,ProjectView,ServerView,ServiceInstanceView,ServiceView } from '../../core/models/api.models';
@Component({standalone:true,imports:[FormsModule,ButtonModule,SelectModule,TableModule,InputTextModule,MessageModule],template:`
<header class="page-header"><div><p class="eyebrow">SCOPED ADMINISTRATION</p><h1>Administration</h1><p>Every mutation is project-authorized by the backend and written to the append-only audit trail.</p></div></header>
<div class="panel"><label>Project to administer</label><p-select class="admin-project" [options]="projects()" optionLabel="name" optionValue="id" [(ngModel)]="projectId" [fluid]="true" placeholder="Choose project" (onChange)="loadProject()" />@if(message()){<p-message [severity]="messageSeverity()">{{message()}}</p-message>}</div>
<div class="admin-columns">
 <section class="panel"><h2>Environments</h2><p-table [value]="environments()"><ng-template #header><tr><th>Code</th><th>Name</th><th>Type</th></tr></ng-template><ng-template #body let-e><tr><td>{{e.code}}</td><td>{{e.name}}</td><td>{{e.type}}</td></tr></ng-template></p-table><h3>Add environment</h3><div class="compact-form"><input pInputText [(ngModel)]="newEnvironment.code" placeholder="Code" /><input pInputText [(ngModel)]="newEnvironment.name" placeholder="Name" /><p-button label="Create" icon="pi pi-plus" (onClick)="createEnvironment()" [disabled]="!ready(newEnvironment.code,newEnvironment.name)" /></div></section>
 <section class="panel"><h2>Logical services</h2><p-table [value]="services()"><ng-template #header><tr><th>Service</th><th>Criticality</th><th>Owner</th></tr></ng-template><ng-template #body let-s><tr><td>{{s.displayName}}</td><td>{{s.criticality}}</td><td>{{s.owner}}</td></tr></ng-template></p-table><h3>Add service</h3><div class="compact-form"><input pInputText [(ngModel)]="newService.key" placeholder="service-key" /><input pInputText [(ngModel)]="newService.name" placeholder="Display name" /><p-button label="Create" icon="pi pi-plus" (onClick)="createService()" [disabled]="!ready(newService.key,newService.name)" /></div></section>
 <section class="panel"><h2>Servers</h2><label>Environment</label><p-select [options]="environments()" optionLabel="code" optionValue="id" [(ngModel)]="environmentId" [fluid]="true" placeholder="Choose environment" (onChange)="loadServers()" /><p-table [value]="servers()"><ng-template #header><tr><th>Host ID</th><th>Hostname</th><th>Status</th></tr></ng-template><ng-template #body let-s><tr><td>{{s.hostId}}</td><td>{{s.hostname}}</td><td>{{s.status}}</td></tr></ng-template></p-table><h3>Add server</h3><div class="compact-form"><input pInputText [(ngModel)]="newServer.hostId" placeholder="host-id" /><input pInputText [(ngModel)]="newServer.hostname" placeholder="hostname.local.test" /><p-button label="Create" icon="pi pi-plus" (onClick)="createServer()" [disabled]="!environmentId||!ready(newServer.hostId,newServer.hostname)" /></div></section>
 <section class="panel"><h2>Service instances</h2><label>Server</label><p-select [options]="servers()" optionLabel="hostname" optionValue="id" [(ngModel)]="serverId" [fluid]="true" placeholder="Choose server" (onChange)="loadInstances()" /><p-table [value]="instances()"><ng-template #header><tr><th>Instance</th><th>Service</th><th>Version</th></tr></ng-template><ng-template #body let-i><tr><td>{{i.instanceKey}}</td><td>{{i.serviceName}}</td><td>{{i.version}}</td></tr></ng-template></p-table><h3>Add instance</h3><div class="stack-form"><p-select [options]="services()" optionLabel="displayName" optionValue="id" [(ngModel)]="newInstance.serviceId" [fluid]="true" placeholder="Logical service" /><input pInputText [(ngModel)]="newInstance.key" placeholder="instance-key" /><input pInputText [(ngModel)]="newInstance.version" placeholder="Version" /><p-button label="Create instance" icon="pi pi-plus" (onClick)="createInstance()" [disabled]="!serverId||!newInstance.serviceId||!newInstance.key" /></div></section>
 <section class="panel"><h2>Log sources</h2><label>Service instance</label><p-select [options]="instances()" optionLabel="instanceKey" optionValue="id" [(ngModel)]="instanceId" [fluid]="true" placeholder="Choose instance" /><div class="stack-form"><input pInputText [(ngModel)]="newSource.path" placeholder="/logs/service/*.log" /><input pInputText [(ngModel)]="newSource.type" placeholder="Log type" /><input pInputText [(ngModel)]="newSource.parser" placeholder="Parser profile" /><p-button label="Create log source" icon="pi pi-plus" (onClick)="createSource()" [disabled]="!instanceId||!newSource.path||!newSource.type||!newSource.parser" /></div></section>
 <section class="panel"><h2>User / project access</h2><p-table [value]="access()"><ng-template #header><tr><th>User</th><th>Role</th></tr></ng-template><ng-template #body let-a><tr><td>{{a.displayName}}<small class="table-sub">{{a.user}}</small></td><td>{{a.role}}</td></tr></ng-template></p-table><h3>Add membership</h3><div class="stack-form"><p-select [options]="users()" optionLabel="displayName" optionValue="id" [(ngModel)]="newAccess.userId" [fluid]="true" placeholder="User" /><p-select [options]="roles" [(ngModel)]="newAccess.role" [fluid]="true" /><p-button label="Grant project role" icon="pi pi-user-plus" (onClick)="createMembership()" [disabled]="!projectId||!newAccess.userId" /></div><p class="help">Environment/service grants and access revocation are available through the administration API and Swagger.</p></section>
</div>`}) export class AdministrationComponent implements OnInit{
 private readonly api=inject(CatalogApiService);projects=signal<ProjectView[]>([]);environments=signal<EnvironmentView[]>([]);services=signal<ServiceView[]>([]);servers=signal<ServerView[]>([]);instances=signal<ServiceInstanceView[]>([]);access=signal<Record<string,unknown>[]>([]);users=signal<{id:string;displayName:string}[]>([]);message=signal('');messageSeverity=signal<'success'|'error'>('success');roles=['PROJECT_ADMIN','PRODUCTION_SUPPORT','DEVELOPER','VIEWER','AUDITOR'];projectId:string|null=null;environmentId:string|null=null;serverId:string|null=null;instanceId:string|null=null;newEnvironment={code:'',name:''};newService={key:'',name:''};newServer={hostId:'',hostname:''};newInstance={serviceId:'',key:'',version:'1.0.0'};newSource={path:'',type:'application',parser:'logback-json'};newAccess={userId:'',role:'VIEWER'};
 ngOnInit(){this.api.projects().subscribe(v=>this.projects.set(v));this.api.adminUsers().subscribe({next:v=>this.users.set(v),error:()=>this.users.set([])});}ready(...values:string[]){return !!this.projectId&&values.every(Boolean);}loadProject(){if(!this.projectId)return;this.api.environments(this.projectId).subscribe(v=>this.environments.set(v));this.api.services(this.projectId).subscribe(v=>this.services.set(v));this.api.access(this.projectId).subscribe({next:v=>this.access.set(v as Record<string,unknown>[]),error:()=>this.denied()});}loadServers(){this.servers.set([]);this.instances.set([]);if(this.environmentId)this.api.servers(this.environmentId).subscribe(v=>this.servers.set(v));}loadInstances(){this.instances.set([]);if(this.serverId)this.api.instances(this.serverId).subscribe(v=>this.instances.set(v));}
 createEnvironment(){this.mutate(this.api.createEnvironment({projectId:this.projectId,code:this.newEnvironment.code,name:this.newEnvironment.name,type:this.newEnvironment.code.toUpperCase()==='PROD'?'PROD':'OTHER',sensitivity:'INTERNAL'}),()=>{this.newEnvironment={code:'',name:''};this.loadProject();},'Environment created');}createService(){this.mutate(this.api.createService({projectId:this.projectId,serviceKey:this.newService.key,displayName:this.newService.name,owner:'Unassigned',criticality:'MEDIUM',labels:{}}),()=>{this.newService={key:'',name:''};this.loadProject();},'Service created');}createServer(){this.mutate(this.api.createServer({environmentId:this.environmentId,hostId:this.newServer.hostId,hostname:this.newServer.hostname,labels:{}}),()=>{this.newServer={hostId:'',hostname:''};this.loadServers();},'Server created');}createInstance(){this.mutate(this.api.createInstance({serviceId:this.newInstance.serviceId,serverId:this.serverId,instanceKey:this.newInstance.key,version:this.newInstance.version}),()=>{this.newInstance={serviceId:'',key:'',version:'1.0.0'};this.loadInstances();},'Service instance created');}createSource(){this.mutate(this.api.createLogSource({serviceInstanceId:this.instanceId,pathPattern:this.newSource.path,logType:this.newSource.type,parserProfile:this.newSource.parser,multilineRule:'^\\s+at ',enabled:true}),()=>this.newSource={path:'',type:'application',parser:'logback-json'},'Log source created');}createMembership(){this.mutate(this.api.createMembership({userId:this.newAccess.userId,projectId:this.projectId,role:this.newAccess.role}),()=>this.loadProject(),'Membership created');}
 private mutate(request:{subscribe:(handlers:{next:()=>void;error:()=>void})=>unknown},after:()=>void,label:string){request.subscribe({next:()=>{this.messageSeverity.set('success');this.message.set(`${label} and audited.`);after();},error:()=>this.denied()});}private denied(){this.messageSeverity.set('error');this.message.set('Operation denied or invalid. Confirm project-admin scope and field values.');}
}
