import { Component,inject,OnInit,signal } from '@angular/core';import { ActivatedRoute } from '@angular/router';import { CardModule } from 'primeng/card';import { SelectModule } from 'primeng/select';import { TableModule } from 'primeng/table';import { FormsModule } from '@angular/forms';import { CatalogApiService } from '../../core/api/catalog-api.service';import { EnvironmentView,LogSourceView,ProjectView,ServerView,ServiceInstanceView,ServiceView } from '../../core/models/api.models';
@Component({standalone:true,imports:[CardModule,SelectModule,TableModule,FormsModule],template:`@if(project();as p){<header class="page-header"><div><p class="eyebrow">PROJECT CATALOG</p><h1>{{p.name}}</h1><p>{{p.classification}} · {{p.status}}</p></div><div class="summary-inline"><span><strong>{{p.environments}}</strong> environments</span><span><strong>{{p.servers}}</strong> servers</span><span><strong>{{p.services}}</strong> services</span><span><strong>{{p.instances}}</strong> instances</span></div></header>}<section class="catalog-grid"><div class="panel"><h2>Topology browser</h2><div class="form-row"><div><label>Environment</label><p-select [options]="environments()" optionLabel="code" optionValue="id" [(ngModel)]="environmentId" placeholder="Choose environment" [fluid]="true" (onChange)="loadServers()" /></div><div><label>Server</label><p-select [options]="servers()" optionLabel="hostname" optionValue="id" [(ngModel)]="serverId" [disabled]="!environmentId" placeholder="Choose server" [fluid]="true" (onChange)="loadInstances()" /></div></div><p-table [value]="instances()"><ng-template #header><tr><th>Service instance</th><th>Service</th><th>Version</th><th>Status</th></tr></ng-template><ng-template #body let-i><tr tabindex="0" (click)="loadSources(i.id)" (keydown.enter)="loadSources(i.id)"><td>{{i.instanceKey}}</td><td>{{i.serviceName}}</td><td>{{i.version}}</td><td>{{i.status}}</td></tr></ng-template></p-table></div><div class="panel"><h2>Log sources</h2>@if(!selectedInstance()){<p class="empty">Select a service instance to inspect its collector-ready log source mappings.</p>}<p-table [value]="sources()"><ng-template #header><tr><th>Path pattern</th><th>Type</th><th>Parser</th><th>Version</th></tr></ng-template><ng-template #body let-s><tr><td><code>{{s.pathPattern}}</code></td><td>{{s.logType}}</td><td>{{s.parserProfile}}</td><td>{{s.configVersion}}</td></tr></ng-template></p-table></div></section><section class="panel"><h2>Logical services</h2><p-table [value]="services()"><ng-template #header><tr><th>Service</th><th>Key</th><th>Owner</th><th>Criticality</th></tr></ng-template><ng-template #body let-s><tr><td>{{s.displayName}}</td><td><code>{{s.serviceKey}}</code></td><td>{{s.owner}}</td><td>{{s.criticality}}</td></tr></ng-template></p-table></section>`}) export class ProjectDetailComponent implements OnInit{private readonly api=inject(CatalogApiService);private readonly route=inject(ActivatedRoute);readonly project=signal<ProjectView|null>(null);readonly environments=signal<EnvironmentView[]>([]);readonly servers=signal<ServerView[]>([]);readonly services=signal<ServiceView[]>([]);readonly instances=signal<ServiceInstanceView[]>([]);readonly sources=signal<LogSourceView[]>([]);readonly selectedInstance=signal<string|null>(null);projectId='';environmentId:string|null=null;serverId:string|null=null;ngOnInit(){this.projectId=this.route.snapshot.paramMap.get('id')!;this.api.project(this.projectId).subscribe(v=>this.project.set(v));this.api.environments(this.projectId).subscribe(v=>this.environments.set(v));this.api.services(this.projectId).subscribe(v=>this.services.set(v));}loadServers(){this.serverId=null;this.instances.set([]);this.sources.set([]);if(this.environmentId)this.api.servers(this.environmentId).subscribe(v=>this.servers.set(v));}loadInstances(){this.sources.set([]);if(this.serverId)this.api.instances(this.serverId).subscribe(v=>this.instances.set(v));}loadSources(id:string){this.selectedInstance.set(id);this.api.logSources(id).subscribe(v=>this.sources.set(v));}}

