import { Component,inject,OnInit,signal } from '@angular/core';import { RouterLink } from '@angular/router';import { TableModule } from 'primeng/table';import { ButtonModule } from 'primeng/button';import { TagModule } from 'primeng/tag';import { CatalogApiService } from '../../core/api/catalog-api.service';import { ProjectView } from '../../core/models/api.models';
@Component({standalone:true,imports:[TableModule,ButtonModule,TagModule,RouterLink],template:`<header class="page-header"><div><p class="eyebrow">AUTHORIZED CATALOG</p><h1>Projects</h1><p>Only projects in your server-derived effective scope appear here.</p></div></header><div class="panel"><p-table [value]="projects()" [loading]="loading()" dataKey="id"><ng-template #header><tr><th>Project</th><th>Classification</th><th>Environments</th><th>Servers</th><th>Services</th><th>Instances</th><th></th></tr></ng-template><ng-template #body let-project><tr><td><strong>{{project.name}}</strong><small class="table-sub">{{project.slug}}</small></td><td><p-tag [value]="project.classification" severity="secondary" /></td><td>{{project.environments}}</td><td>{{project.servers}}</td><td>{{project.services}}</td><td>{{project.instances}}</td><td><a pButton [routerLink]="['/projects',project.id]" class="p-button-text"><span pButtonLabel>Explore</span><i class="pi pi-arrow-right" pButtonIcon></i></a></td></tr></ng-template></p-table></div>`}) export class ProjectsComponent implements OnInit{private readonly api=inject(CatalogApiService);projects=signal<ProjectView[]>([]);loading=signal(true);ngOnInit(){this.api.projects().subscribe(v=>{this.projects.set(v);this.loading.set(false);});}}

